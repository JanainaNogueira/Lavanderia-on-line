import { NomeDirective } from './nome.directive';

describe('NomeDirective', () => {
  it('should create an instance', () => {
    const directive = new NomeDirective();
    expect(directive).toBeTruthy();
  });
});
