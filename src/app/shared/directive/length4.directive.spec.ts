import { length4Directive } from './length4.directive';

describe('length4Directive', () => {
  it('should create an instance', () => {
    const directive = new length4Directive();
    expect(directive).toBeTruthy();
  });
});
